{
  "name": "embed-iframe-sdk",
  "packageManager": "yarn@4.9.1",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/**"
    ]
  },
  "scripts": {
    "dev": "yarn workspaces foreach --all -pi run dev"
  }
}
